# @package _global_
# Budget for Bayesian Optimization
# One run equals one agent evaluation with n_eval_episodes


# Evaluation
n_eval_episodes: ${eval_contexts.num_contexts}

# Environment
env: CARLPendulumEnv
